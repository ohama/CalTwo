---
phase: 03-ui-implementation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tutorial/phase-03.md
autonomous: true

must_haves:
  truths:
    - "Korean tutorial exists at tutorial/phase-03.md"
    - "Tutorial explains Feliz view functions and CSS styling step-by-step"
    - "Tutorial includes code examples for CSS Grid layout, keyboard events, and Backspace"
    - "Beginner developer can follow tutorial to understand Phase 3 implementation"
  artifacts:
    - path: "tutorial/phase-03.md"
      provides: "Korean tutorial for Phase 3 UI implementation"
      min_lines: 400
  key_links: []
---

<objective>
Write comprehensive Korean tutorial explaining Phase 3 UI implementation: Feliz view functions, CSS Grid layout, external CSS for hover/active states, keyboard event handling, and Backspace functionality.

Purpose: Korean-language learning resource that helps beginner developers understand how Feliz renders UI and how CSS Grid creates calculator layouts.
Output: tutorial/phase-03.md with step-by-step explanations and code examples.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-implementation/03-01-SUMMARY.md

@tutorial/phase-01.md
@tutorial/phase-02.md
@src/Calculator.fs
@src/App.fs
@src/Main.fs
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Korean tutorial for Phase 3 UI implementation</name>
  <files>tutorial/phase-03.md</files>
  <action>
Create `tutorial/phase-03.md` following the same style and depth as `tutorial/phase-01.md` and `tutorial/phase-02.md`.

Structure (all content in Korean):

1. **Phase 3 개요** — What this phase accomplishes (CSS Grid UI, visual feedback, keyboard support, Backspace)

2. **Feliz 뷰 함수 이해하기** — How Feliz translates F# to React elements
   - Html.div, Html.button, prop.text, prop.onClick
   - prop.style for inline styles vs prop.className for CSS classes
   - prop.children for nesting elements
   - Code examples from the actual App.fs view function

3. **CSS Grid 레이아웃** — How CSS Grid creates the calculator button grid
   - What CSS Grid is (2D layout system)
   - `grid-template-columns: repeat(4, 1fr)` explained
   - How buttons flow into grid cells automatically
   - `gridColumn "1 / 3"` for spanning the 0 button across 2 columns
   - Feliz syntax: `style.display.grid`, `style.gridTemplateColumns`
   - Compare: why Grid over Flexbox for calculator layout

4. **외부 CSS와 호버/액티브 효과** — External CSS for pseudo-classes
   - Why inline styles can't do `:hover` and `:active`
   - How to create styles.css with button classes
   - `importSideEffects "./styles.css"` in Main.fs
   - CSS transition for smooth animation
   - Code walkthrough of each button class (calc-button, calc-operator, calc-equals, calc-clear)

5. **키보드 이벤트 처리** — Keyboard event handling
   - `prop.onKeyDown` and `prop.tabIndex 0`
   - Pattern matching on `e.key` (string, not deprecated keyCode)
   - `e.preventDefault()` to prevent browser defaults (/ opens search, Backspace navigates back)
   - Code example of the handleKeyDown function

6. **Backspace 기능 구현** — Backspace implementation
   - New `BackspacePressed` message in Msg type
   - F# string slicing: `display.[0..display.Length-2]`
   - Edge cases: "0", "Error", single character
   - Unit tests for Backspace behavior

7. **반응형 디자인** — Responsive design
   - `fr` units for fluid columns
   - `maxWidth 400` + `margin auto` for centering
   - `width 90%` for mobile viewport
   - CSS media queries for mobile touch targets (48px)
   - WCAG 2.1 AAA: 44px minimum touch target size

8. **전체 코드 정리** — Complete code review
   - Full Calculator.fs (with BackspacePressed)
   - Full App.fs (with CSS Grid view + keyboard handler)
   - Full styles.css
   - Full Main.fs (with importSideEffects)

9. **정리 및 다음 단계** — Summary and what's next (Phase 4: E2E Testing)

Key guidelines:
- ALL text in Korean (technical terms can be in English with Korean explanation)
- Follow the exact style of phase-01.md and phase-02.md
- Include actual code from the completed source files (not hypothetical code)
- Explain WHY each decision was made, not just WHAT was done
- Target audience: beginner developer learning F# + web development
- Include command examples (`npm run dev`, `npm test`)
  </action>
  <verify>
1. File exists at `tutorial/phase-03.md`
2. File is at least 400 lines (comprehensive tutorial)
3. Content is in Korean
4. Contains code examples from actual source files
5. Covers all 9 sections listed above
  </verify>
  <done>
tutorial/phase-03.md exists with 400+ lines of Korean content explaining Feliz view functions, CSS Grid layout, external CSS, keyboard events, Backspace implementation, and responsive design with code examples from the completed Phase 3 source files.
  </done>
</task>

</tasks>

<verification>
1. `cat tutorial/phase-03.md | wc -l` shows 400+ lines
2. Tutorial is entirely in Korean (with English technical terms)
3. Code examples match actual source files from Phase 3
4. All 9 sections are present
</verification>

<success_criteria>
- tutorial/phase-03.md exists with comprehensive Korean content
- Tutorial explains Feliz views, CSS Grid, hover/active CSS, keyboard events, Backspace, responsive design
- Beginner developer can follow step-by-step to understand Phase 3 code
- Satisfies TUT-01, TUT-02, TUT-03 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-implementation/03-02-SUMMARY.md`
</output>
